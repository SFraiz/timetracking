<h1>Add a project</h1>
<%= form_for @project do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>

	<p><%= f.label :description %>
	<%= f.text_area :description %></p>
	<p><%= f.button %></p>
<% end %>